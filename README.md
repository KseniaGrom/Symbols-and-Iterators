Класс ErrorRepository внутри которого хранятся Map, в котором ключ - это код ошибки (число), а значение - текстовое описание

Метод translate(code), позволяет по коду получить текст ошибки, а в случае отсутствия подобного кода возвращать строку 'Unknown error'.